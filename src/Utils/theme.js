import { createTheme } from "@mui/material/styles";
import { Colors } from "Utils/constants";

export default createTheme({
	palette: {
		primary: {
			main: Colors.DODGER_BLUE,
		},
		secondary: {
			main: "#fefefe",
		},
	},
});
